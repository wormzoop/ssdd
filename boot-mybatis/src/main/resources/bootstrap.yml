server:
  port: 8080
spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      uri: http://localhost:8888
mybatis:
  config-location: classpath:mybatis-config.xml        
       