spring:
  application:
    name: boot-mybatis-annotation
  cloud:
    config:
      uri: http://localhost:8888
      name: boot-mybatis-annotation
      profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    appname: boot-mybatis-annotation